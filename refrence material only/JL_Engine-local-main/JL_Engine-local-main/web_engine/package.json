{
  "name": "jl-web-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-native JL Engine core — TypeScript port of the behavioral loop, sharing the MPF operator spec with the Julia and Python engines.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:ext": "tsc && vite build --mode extension",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
